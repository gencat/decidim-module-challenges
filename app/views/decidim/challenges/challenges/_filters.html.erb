<%= filter_form_for filter do |form| %>
  <div class="filters__section">
    <div class="filters__search">
      <div class="input-group">
        <%= form.search_field :search_text, label: false, class: "input-group-field", placeholder: t(".search") %>
        <div class="input-group-button">
          <button type="submit" class="button button--muted">
            <%= icon "magnifying-glass", aria_label: t(".search") %>
          </button>
        </div>
      </div>
    </div>
  </div>

  <%= form.check_boxes_tree :state, filter_challenges_state_values, legend_title: t(".state") %>

  <% if current_participatory_space.has_subscopes? %>
    <%= form.check_boxes_tree :scope_id, filter_scopes_values, legend_title: t(".scope") %>
  <% end %>

  <% if current_component.categories.any? %>
    <%= form.check_boxes_tree :category_id, filter_categories_values, legend_title: t(".category") %>
  <% end %>

  <%# byebug %>
  <% if current_participatory_space.components.published.where(manifest_name: "sdgs").any? %>
  <div class="filters__section">
    <div id="sdg-ids-selector" class="data-picker picker-multiple" data-picker-name="filter[sdgs_ids]">
      <div class="picker-values">
        <div><a href="<%= params[:url] %>" data-picker-value="<%= 666 %>"><%= "params[:text]!" %></a></div>
      </div>
      <div class="picker-prompt"><a href="#"><%= t(".select") %></a></div>
    </div>


    <%           picker_options = {
            id: sanitize_to_id("sdgs_ids"),
            class: "picker-multiple",
            name: "#{form.object_name}[#{"sdgs_ids"}]",
            multiple: true,
            autosort: true
          }
          prompt_params = {
            url: data_picker_modal_content_challenges_path,
            text: t(".select")
          }
 %>
    <%= form.data_picker(:sdgs_ids, picker_options, prompt_params) do |item|
            { url: data_picker_modal_content_challenges_path, text: present(item).id_and_title }
          end %>
  </div>
  <% end %>

  <%# if linked_classes_for(Decidim::Proposals::Proposal).any? %>
    <%#= form.collection_radio_buttons :related_to, linked_classes_filter_values_for(Decidim::Proposals::Proposal), :first, :last, legend_title: t(".related_to") %>
  <%# end %>

  <%= hidden_field_tag :order, order, id: nil, class: "order_filter" %>
<% end %>
